--- a/apps/jscad-web/examples/openscad/dotscad/experimental/tile_truchet.scad	2026-03-31 10:23:54.055266574 -0400
+++ b/apps/jscad-web/examples/openscad/dotscad/experimental/tile_truchet.scad	2026-04-06 09:25:12.395357334 -0400
@@ -17,17 +17,10 @@
 				[for(x = x_range) my[x]]
 		]
 	)
-	is_undef(seed) ? [
+	[
         for(y = y_range)
-        let(my = m[y])      
-            for(x = x_range)  
+        let(my = m[y])
+            for(x = x_range)
             if(my[x] == 1)
-                [x, y, choose(nums)]
-        
-    ] : [
-        for(y = y_range)  
-        let(my = m[y])      
-            for(x = x_range) 
-            if(my[x] == 1)
-            [x, y, choose(nums, x + y * rows + seed)]
+            [x, y, choose(nums, x + y * rows + (is_undef(seed) ? 0 : seed))]
     ];
\ No newline at end of file
