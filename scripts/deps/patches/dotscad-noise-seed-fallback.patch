--- a/apps/jscad-web/examples/openscad/dotscad/noise/nz_perlin1s.scad	2026-03-31 10:23:54.000000000 -0400
+++ b/apps/jscad-web/examples/openscad/dotscad/noise/nz_perlin1s.scad	2026-04-06 14:00:00.000000000 -0400
@@ -12,5 +12,5 @@ use <../util/rand.scad>
 use <_impl/_pnoise1_impl.scad>
 
 function nz_perlin1s(xs, seed) = 
-    let(sd = is_undef(seed) ? rand() * 1000: seed)
+    let(sd = is_undef(seed) ? 0 : seed)
     [for(x = xs) _pnoise1_impl(x, sd)];
--- a/apps/jscad-web/examples/openscad/dotscad/noise/nz_perlin2s.scad	2026-03-31 10:23:54.000000000 -0400
+++ b/apps/jscad-web/examples/openscad/dotscad/noise/nz_perlin2s.scad	2026-04-06 14:00:00.000000000 -0400
@@ -12,5 +12,5 @@ use <../util/rand.scad>
 use <_impl/_pnoise2_impl.scad>
 
 function nz_perlin2s(points, seed) = 
-    let(sd = is_undef(seed) ? rand() * 1000 : seed)
+    let(sd = is_undef(seed) ? 0 : seed)
     [for(p = points) _pnoise2(p.x, p.y, sd)];    
--- a/apps/jscad-web/examples/openscad/dotscad/noise/nz_perlin3s.scad	2026-03-31 10:23:54.000000000 -0400
+++ b/apps/jscad-web/examples/openscad/dotscad/noise/nz_perlin3s.scad	2026-04-06 14:00:00.000000000 -0400
@@ -12,5 +12,5 @@ use <../util/rand.scad>
 use <_impl/_pnoise3_impl.scad>
 
 function nz_perlin3s(points, seed) = 
-    let(sd = is_undef(seed) ? rand() * 1000 : seed)
+    let(sd = is_undef(seed) ? 0 : seed)
     [for(p = points) _pnoise3(p.x, p.y, p.z, sd)];    
--- a/apps/jscad-web/examples/openscad/dotscad/noise/nz_worley2s.scad	2026-03-31 10:23:54.000000000 -0400
+++ b/apps/jscad-web/examples/openscad/dotscad/noise/nz_worley2s.scad	2026-04-06 14:00:00.000000000 -0400
@@ -12,5 +12,5 @@ use <../util/rand.scad>
 use <_impl/_nz_worley2_impl.scad>
 
 function nz_worley2s(points, seed, grid_w = 10, dist = "euclidean") =
-    let(sd = is_undef(seed) ? rand() * 1000 : seed)
+    let(sd = is_undef(seed) ? 0 : seed)
     _nz_worley2s(points, sd, grid_w, dist);
--- a/apps/jscad-web/examples/openscad/dotscad/noise/nz_worley3s.scad	2026-03-31 10:23:54.000000000 -0400
+++ b/apps/jscad-web/examples/openscad/dotscad/noise/nz_worley3s.scad	2026-04-06 14:00:00.000000000 -0400
@@ -12,5 +12,5 @@ use <../util/rand.scad>
 use <_impl/_nz_worley3_impl.scad>
 
 function nz_worley3s(points, seed, grid_w = 10, dist = "euclidean") =
-    let(sd = is_undef(seed) ? rand() * 1000: seed)
+    let(sd = is_undef(seed) ? 0 : seed)
     _nz_worley3s(points, sd, grid_w, dist);
