node_modules
build
build_dev
test-results/
playwright-report/
# Local rowboat tenant scratch (provision:local): the state file carries the
# tenant management key; the env file is derived from it.
.rowboat-dev/
rowboat-tenant.local.json
.env.tenant.local