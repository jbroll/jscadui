# Echo skip list: models whose geometry test-harness.js grades but whose echo()
# output it does not compare. Only test-harness.js reads this.

# shapes.scad echoes a string literal that spans lines. OpenSCAD's lexer drops
# a raw newline inside a string literal; openscad-parser keeps it, so the
# transpiled echo has "\n" where OpenSCAD has none. Parser bug (jbroll/openscad-parser).
examples/shapes_3d.scad
