# dotSCAD skip list — add filenames (relative to dotscad/) of known failures
# Format: one pattern per line, # for comments
# Supports * wildcards and trailing / as directory shorthand

# Non-deterministic models: these use OpenSCAD's unseeded rands() which is
# time-seeded and produces different results each run. The reference STL is
# generated once, but our transpiler (and any re-run of OpenSCAD) would
# generate different geometry. These models can never reliably reach 0.99.

# Maze models: all now deterministic via position-based seeding in rand_dirs.
# island_maze and maze_yinyan have explicit seeds but fail due to transpiler bug — kept in test suite.

# Unseeded-rands models: use time-seeded rands() with no deterministic seed,
# so geometry differs between reference STL (generated once) and test runs.
examples/tiles/random_town_square.scad
examples/tiles/penrose_basket.scad
examples/voronoi/ruyi_pineapple.scad
examples/crystal_cluster.scad
examples/differential_line_growth.scad
examples/differential_line_growth_bowl.scad
examples/turtle/tree.scad
examples/voronoi/ripple_vase.scad
examples/taiwan/chair_score.scad
examples/tiles/random_city.scad
examples/taiwan/random_city_taiwan.scad
# Wang tile models: tile_w2c/tile_w2e use unseeded rands() for corner/edge assignment
examples/tiles/2_corner_wang_tiles_basic.scad
examples/tiles/2_edge_wang_tiles_basic.scad
# tube_box uses tile_w2e which uses unseeded rands()
examples/tiles/tube_box.scad
# hollow_out_holder uses hollow_out_square which calls rand() without seed
examples/hollow_out/hollow_out_holder.scad
# rock_theta_maze: rock() module uses unseeded rands() for rock shape generation
examples/maze/rock_theta_maze.scad

# Missing dependency: shape_glued2circles.scad not included in dotSCAD repo
examples/spiral/spring_dog.scad

# OOM: model geometry is extremely complex and exhausts available memory
examples/fidget_ball_fern_leaf.scad

# Timeout: model renders too slowly in JSCAD (complex recursive/iterative geometry)
examples/spiral/climbing_rose.scad
examples/stereographic_projection/stereographic_foliage_scroll.scad

# Font not available: these models use fonts that are not available in the JSCAD runtime.
# The rendered text geometry differs because a fallback font is used instead.
# stereographic_chars uses "Webdings" — a Windows symbol font not in JSCAD runtime.
examples/stereographic_projection/stereographic_chars.scad

# Non-manifold reference STL: OpenSCAD produces non-manifold geometry for these models
# (compare-stl.js trianglesToManifold fails); no valid comparison is possible.
examples/voronoi/ripple_sphere.scad
examples/voronoi/voronoi_vase.scad
