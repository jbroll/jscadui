# dotSCAD skip list — models that do not render, excluded from every sweep, the
# demo browser and the ALL.js grids. A model that renders but that the STL
# comparison cannot grade goes in compare-skip.txt instead.
# Format: one pattern per line, # for comments
# Supports * wildcards and trailing / as directory shorthand

# Missing dependency: shape_glued2circles.scad not included in dotSCAD repo
examples/spiral/spring_dog.scad

# OOM: model geometry is extremely complex and exhausts available memory
examples/fidget_ball_fern_leaf.scad

# Timeout: model renders too slowly in JSCAD (complex recursive/iterative geometry)
examples/spiral/climbing_rose.scad
examples/stereographic_projection/stereographic_foliage_scroll.scad
examples/differential_line_growth_bowl.scad

# These reference library files that upstream dotSCAD does not ship at the
# pinned commit (bb33edfd): neither util/rands_disk.scad nor maze/mz_wang_tiles.scad
# exists anywhere in the source tree. Broken at source, not by our vendoring.
examples/circle_packing/forest.scad
examples/taiwan/maze_city_taiwan.scad

# OpenSCAD cannot render this either: dotSCAD's _delaunayBoundaries recurses
# without end on its point set, and OpenSCAD stops with "Recursion detected
# calling function '_delaunayBoundaries'" after ~25s. Our trampoline turns that
# runaway tail recursion into a loop that never grows the stack, so here it
# spins until the frame's model budget kills it instead.
examples/voronoi/voronoi_melon.scad
