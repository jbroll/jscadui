# Text examples skip list
# Text rendering uses Hershey stroke fonts which produce different glyph
# geometry than OpenSCAD's TrueType fonts. Jaccard similarity is very low
# (~0.09) even when the script runs without error. Not a transpiler bug.

text_basic.scad
text-fonts.scad
