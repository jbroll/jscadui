# Text examples skip list
# text_basic.scad and text-fonts.scad now use Liberation Sans TTF (via bundle.openscad.js),
# matching OpenSCAD's default font. They pass the Jaccard threshold (≥0.99).
# Other text files produce 2D-only output which OpenSCAD cannot export as STL.
