# BOSL v1 skip list
# Files listed here are excluded from test-harness runs.
# Format: one filename per line, comments start with #

# Uses rands() with global state - our MT19937 produces different random numbers than
# OpenSCAD's implementation, so the generated hull/sphere positions don't match.
# The file runs without error but geometry differs due to rands() output differences.
test_convex_hull.scad
